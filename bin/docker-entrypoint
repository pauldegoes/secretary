#!/bin/bash -e

# Compile assets before starting Rails server
if [ "${1}" == "./bin/rails" ] && [ "${2}" == "server" ]; then
  echo "🔨 Compiling assets before server startup..."
  ./bin/compile-assets
  ./bin/rails db:prepare
fi

exec "${@}"
